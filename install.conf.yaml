- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zim: zim
    ~/.zimrc: .zimrc
    ~/.zshrc: .zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]